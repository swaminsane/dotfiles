#!/bin/sh

[ -f "$HOME/.profile" ] && . "$HOME/.profile"

export WM="xfce"

if [ -z "$DBUS_SESSION_BUS_ADDRESS" ]; then
    eval "$(dbus-launch --sh-syntax --exit-with-session)"
fi

[ -f "$HOME/.xprofile" ] && . "$HOME/.xprofile"

exec startxfce4 > "$HOME/xfce.log" 2>&1
